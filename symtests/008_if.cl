class Main {
    main() : SELF_TYPE {
        if let enLaCondicion : Bool in enLaCondicion then
           let valorEntero : Int <- getInteger() in valor
        else
            let valorString : String <- getString() in valor
        fi
    };
};